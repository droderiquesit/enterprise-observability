# Runbook: API Latency Seasonal Anomaly

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `api-latency-seasonal` |
| Domain | API (owner `application-development`) |
| Signal | `latency` · impact class `degradation` |
| Detection | `seasonal_anomaly` |
| Environments | prod |
| Priority | P2 · does not page |
| SLO | `slo-api-latency` — API latency objective (target 99.5%) |
| Automation | `diag-api-health` (diagnostic only) |
| Grouping | `service` · collapse: none |
| Failure domain | `api-services` (correlation key prefix) |

## Meaning

This monitor fired because the signal deviated from its learned SEASONAL baseline — same hour, same day of week. Normal daily and weekly cycles are already accounted for.

Query:

```
avg(last_30m):anomalies(p95:trace.http.request.duration{__SCOPE__} by {service}, 'robust', 3, direction='above', seasonality='weekly') >= 1
```

## Impact

The service still functions but measurably worse; a subset of traffic or users is failing.

Error budget: this condition is associated with `slo-api-latency`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `diag-api-health` (diagnostic only) runs automatically on every alert.

Actions: `fetch_monitor`, `top_failing_endpoints`, `upstream_status`, `rate_limit_state`.

Attached to the alert automatically: top failing routes, upstream health, current rate-limit headroom.

## Escalation

1. Owning team: `application-development` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `sre`.
3. Priority contract: P2 — ack within 10 minutes, escalate at 20.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `api-services`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

