# Runbook: Application Stopped Reporting Telemetry

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `app-telemetry-loss` |
| Domain | Application |
| Signal | `telemetry_health` · impact class `hygiene` |
| Detection | `threshold` |
| Monitor type | `query alert` |
| Environments | prod, qa, stage |
| Priority | P4 · does not page |
| SLO | `slo-app-availability` — Application availability (target 99.9%) |
| Automation | `diag-app-health` (diagnostic only) |
| Grouping | `service` · collapse: none |
| Failure domain | `application-platform` |

## Purpose and monitored condition

This monitor watches **telemetry health** for `service` resources in the Application domain. It measures whether the platform can still see the estate — the monitoring of the monitoring.

It fires when a fixed threshold was crossed, evaluated over `last_30m`.

Condition as deployed:

```
sum(last_30m):sum:trace.span.hits{__SCOPE__} by {service}.as_count() < 1
```

Thresholds: `critical` = 1.

Scope selector: ``. The alert is evaluated per `service`.

**Reading the trigger.** A fixed threshold is a judgement about what matters, not a law of nature. If the value is barely over and the trend is flat, the threshold may be the problem — raise that through review rather than muting the alert.

**Why this threshold is a fixed number.** Absence of telemetry is a binary condition. This is the monitor that guarantees "no alerts" never silently means "no data".

## Business and technical impact

**Business impact.** No direct customer impact. Our ability to see, govern or recover the estate is reduced, which raises the cost and duration of the next real incident.

**Technical impact.** Coverage, configuration or recoverability has drifted from the declared standard. The immediate effect is invisible, which is precisely why it is worth fixing now.

**Error budget.** This condition is associated with `slo-app-availability` (Application availability, target 99.9% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `application-platform` in `prod`, and anything downstream of `application-platform`, `web-frontends`, `worker-platform`.

## Likely causes

Most frequent first.

1. **Agent or collector stopped.** The shipper died, lost credentials, or was removed by a rollout.
2. **A pipeline or integration change.** A processor, filter, index or exclusion was changed and is now dropping data.
3. **Credential or quota loss.** The API key was rotated or the ingest quota was exhausted, so data is rejected.
4. **The source genuinely stopped.** The workload was decommissioned or scaled to zero — absence of telemetry is correct and the monitor should be retired.
5. **Network path loss to the collector.** Egress, proxy or firewall change.

## Investigation steps

1. Read the alert's group tags (`service`) to establish scope before opening anything else — they name the affected member directly.
2. Decide first whether the source still exists. Silent decommissioning and silent breakage look identical and have opposite responses.
3. Check the agent or integration status on a sample of the affected members.
4. Check the ingest pipeline for recent changes: exclusion filters, sampling, index limits, quota.
5. Compare against a neighbouring source that is still reporting to separate 'this source' from 'this pipeline'.
6. Read what `diag-app-health` already attached to the alert (error samples, last 3 deploys, dependency health summary) before re-running those checks by hand.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `trace.span.hits`.

**Logs.** `service:application-platform env:prod status:error`

**Traces.** APM > Traces, filtered to `service:application-platform env:prod`, sorted by duration or filtered to errored spans.

**Where to look.** APM traces and service pages are the primary surface. Use the service map to see which dependency is contributing the failure, and the endpoint list to find whether it is concentrated on one route.

**Dashboards.** *Domain — Application* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `application-platform`, `web-frontends`, `worker-platform`. Correlated alerts share the prefix `application-platform.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. Restore the collector or credential. Blast radius: none — this restores visibility, it does not change the workload.
2. Reverse the pipeline change that dropped the data.
3. If the source is genuinely gone, retire the monitor and the resource record so the gap stops alerting.

**Automation.** `diag-app-health` — diagnostic only, read-only, approval: none.
Actions: `fetch_monitor`, `query_recent_errors`, `fetch_deploy_history`, `fetch_dependency_map`.
Attached to the alert automatically: error samples, last 3 deploys, dependency health summary.

## Rollback procedure

Reverting a pipeline change restores the previous volume immediately; if ingest cost is the reason the filter existed, re-apply a narrower version rather than the original blanket filter.

Whatever is changed, record it on the incident. If `diag-app-health` performed an action, review its run history before making further changes. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when the expected volume has resumed for a full evaluation window, the coverage report shows no gap for this source, and any monitor muted during the outage has been unmuted.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_30m`).
2. `slo-app-availability` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P4. At P4: acknowledge within 0 minutes, escalate at 0.

1. **Owning team**, `sre` — notified, not paged.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `sre` — after 20 minutes.
4. **Incident commander / platform leadership** (`platform-leadership`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Site Reliability Engineering** (`sre`) — sre@acme.example

- Alert channel: `teams-sre-alerts` · low-noise: `teams-sre-signals`
- Escalates to: `platform-leadership`
- Domains owned: application, saas, platform
- Support model: business hours

## ServiceNow service and CI

- **Configuration item (CI):** `application-platform` — the failure domain this monitor is scoped to.
- **Business service:** `application-platform`
- **Assignment group:** `SRE_OPERATIONS`
- **Record type:** Task. This archetype does not raise an incident automatically.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Site Reliability Engineering (`sre`) — sre@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/application.yaml` → `app-telemetry-loss`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->
