# Runbook: Critical OS Patches Missing

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `os-critical-patch-missing` |
| Domain | Infrastructure (host / VM / storage) |
| Signal | `drift` · impact class `hygiene` |
| Detection | `threshold` |
| Monitor type | `query alert` |
| Environments | prod, stage |
| Priority | P4 · does not page |
| SLO | `slo-security-telemetry` — Security telemetry continuity (target 99.5%) |
| Automation | `auto-compliance-ticket` (fully automatic) |
| Grouping | `cluster`, `host` · collapse: `cluster` |
| Failure domain | `compute` |

## Purpose and monitored condition

This monitor watches **drift** for `host` resources in the Infrastructure (host / VM / storage) domain. It measures that live configuration no longer matches the declared, reviewed source of truth.

It fires when a fixed threshold was crossed, evaluated over `last_1d`.

Condition as deployed:

```
max(last_1d):max:acme.compliance.missing_critical_patches{__SCOPE__} by {cluster,host} > 0
```

Thresholds: `critical` = 0.

Scope selector: ``. The alert is evaluated per `cluster`, `host` and notifications collapse on `cluster`.

**Reading the trigger.** A fixed threshold is a judgement about what matters, not a law of nature. If the value is barely over and the trend is flat, the threshold may be the problem — raise that through review rather than muting the alert.

**Why this threshold is a fixed number.** A missing critical patch is a known exploitable defect with a published fix. There is no tolerance band to tune — the only question is how long it has been missing, which the runbook covers against the patch SLA.

## Business and technical impact

**Business impact.** No direct customer impact. Our ability to see, govern or recover the estate is reduced, which raises the cost and duration of the next real incident.

**Technical impact.** Coverage, configuration or recoverability has drifted from the declared standard. The immediate effect is invisible, which is precisely why it is worth fixing now.

**Error budget.** This condition is associated with `slo-security-telemetry` (Security telemetry continuity, target 99.5% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `compute` in `prod`, and anything downstream of `compute`, `storage`, `backup-platform`.

## Likely causes

Most frequent first.

1. **A manual change made during an incident.** The most common and most forgivable — it still has to be reconciled.
2. **A change made outside the pipeline.** Console or CLI edit that bypassed review.
3. **An automated process outside this platform.** Another controller managing the same object.
4. **A failed or partial apply.** The pipeline itself left the object half-changed.

## Investigation steps

1. Read the alert's group tags (`cluster`, `host`) to establish scope before opening anything else — they name the affected member directly.
2. Identify exactly which fields differ — drift is only actionable at field level.
3. Find who made the change and when, from the audit trail.
4. Decide whether the live state or the declared state is correct. Sometimes the emergency change was right and the code is wrong.
5. Check whether the same drift exists in other environments.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `acme.compliance.missing_critical_patches`.

**Logs.** `host:* service:compute env:prod`

**Traces.** Not usually applicable at host level; use process metrics and system logs.

**Where to look.** The host or VM page shows the process list, resource usage and the agent's own health. Compare against neighbouring hosts in the same group.

**Dashboards.** *Domain — Infrastructure (host / VM / storage)* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `compute`, `storage`, `backup-platform`, `identity`. Correlated alerts share the prefix `compute.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. If the declared state is correct, re-apply it through the pipeline. Blast radius: the live change is reverted — confirm it is not load-bearing first.
2. If the live state is correct, codify it in the repository and apply through review so the two agree.
3. Never resolve drift by silencing the check.

**Automation.** `auto-compliance-ticket` — fully automatic, approval: none.
Actions: `capture_evidence`, `create_servicenow_task`, `notify_compliance`.
Rate cap: 50 actions/hour.

## Rollback procedure

Re-applying declared state is itself reversible: the previous live values are in the audit trail and the prior state file. If the re-apply breaks something, restore the recorded live values and reconcile the code instead.

Whatever is changed, record it on the incident. If `auto-compliance-ticket` performed an automatic action, it is reversible by design — check its run history for the compensating action. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when a plan against the live estate is empty, the change is either codified or reverted, and the audit trail records which was chosen and why.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_1d`).
2. `slo-security-telemetry` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P4. At P4: acknowledge within 0 minutes, escalate at 0.

1. **Owning team**, `infrastructure-engineering` — notified, not paged.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `infrastructure-engineering` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Infrastructure Engineering** (`infrastructure-engineering`) — infra@acme.example

- Alert channel: `teams-infra-alerts` · low-noise: `teams-infra-signals`
- Escalates to: `sre`
- Domains owned: infrastructure, vmware, network
- Support model: business hours

## ServiceNow service and CI

- **Configuration item (CI):** `compute` — the failure domain this monitor is scoped to.
- **Business service:** `security-telemetry`
- **Assignment group:** `INFRASTRUCTURE`
- **Record type:** Task. This archetype does not raise an incident automatically.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Infrastructure Engineering (`infrastructure-engineering`) — infra@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/infrastructure.yaml` → `os-critical-patch-missing`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->

