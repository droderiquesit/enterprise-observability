# Runbook: Unowned Resource Backlog

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `unowned-resource-backlog` |
| Domain | Shared Platform Services (owner `observability-platform`) |
| Signal | `drift` · impact class `hygiene` |
| Detection | `threshold` |
| Environments | prod |
| Priority | P4 · does not page |
| SLO | `slo-platform-services` — Shared platform services availability (target 99.9%) |
| Automation | `auto-ticket-sustained` (fully automatic) |
| Grouping | not grouped · collapse: none |
| Failure domain | `observability-platform` (correlation key prefix) |

## Meaning

This monitor fired because a fixed threshold was crossed.

Query:

```
max(last_6h):max:acme.observability.unowned_resources_over_sla{__SCOPE__} > 0
```

**Why this threshold is a fixed number.** Resources past the 14-day ownership SLA. Unowned means unactionable — an alert nobody receives is worse than no alert.

## Impact

Observability or configuration hygiene is degraded. No direct customer impact, but our ability to see or govern the estate is reduced.

Error budget: this condition is associated with `slo-platform-services`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `auto-ticket-sustained` (fully automatic) runs when triggered.

Actions: `create_servicenow_task`, `link_monitor`, `notify_team_channel`.

## Escalation

1. Owning team: `observability-platform` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `sre`.
3. Priority contract: P4 — next business day; this never pages.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `observability-platform`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

