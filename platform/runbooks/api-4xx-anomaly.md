# Runbook: API 4xx Client Error Anomaly

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `api-4xx-anomaly` |
| Domain | API |
| Signal | `error_rate` · impact class `degradation` |
| Detection | `anomaly` |
| Monitor type | `query alert` |
| Environments | prod |
| Priority | P2 · does not page |
| SLO | `slo-api-availability` — API availability (target 99.95%) |
| Automation | `diag-api-health` (diagnostic only) |
| Grouping | `service` · collapse: none |
| Failure domain | `api-services` |

## Purpose and monitored condition

This monitor watches **error rate** for `service` resources in the API domain. It measures the proportion or count of requests that failed, not how slow they were.

It fires when the signal deviated from its own learned baseline by more than the configured number of deviations. It did NOT cross a fixed number, evaluated over `last_30m`.

Condition as deployed:

```
avg(last_30m):anomalies(sum:trace.http.request.hits{__SCOPE__,http.status_class:4xx} by {service}.as_rate(), 'agile', 4) >= 1
```

Thresholds: `critical` = 1.0.

Scope selector: `service_archetype:api`. The alert is evaluated per `service`.

**Reading the trigger.** 'The value looks normal to me' is not a reason to close this — the baseline is what moved. Conversely, a recent step change in traffic can make a healthy service look anomalous for a day while the baseline relearns.

**Design note.** 4xx is usually a caller problem, so this is deliberately less sensitive (4 deviations, 30m) and never customer_impact — it exists to catch broken client releases and auth misconfiguration, not to page.

## Business and technical impact

**Business impact.** The service still functions but measurably worse; a subset of traffic, users or records is failing or delayed.

**Technical impact.** Error budget is being consumed and latency or failure is propagating to callers. Retries amplify the load, so a degradation can become an outage without any new fault.

**Error budget.** This condition is associated with `slo-api-availability` (API availability, target 99.95% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `api-services` in `prod`, and anything downstream of `api-gateway`, `api-services`, `partner-apis`.

## Likely causes

Most frequent first.

1. **A deployment.** The most common cause of a step change in errors. Check the correlated change events first.
2. **A downstream dependency failing.** Errors surfacing here are often produced elsewhere; the failing call is usually visible in the trace.
3. **Bad or unexpected input.** A partner, client version or upstream schema change is sending requests the service rejects.
4. **Resource exhaustion.** Connection pool, thread pool or rate limiter is shedding load, which surfaces as errors rather than latency.
5. **Authentication or certificate expiry.** Credentials, tokens or TLS material rotated or expired and calls now fail closed.

## Investigation steps

1. Read the alert's group tags (`service`) to establish scope before opening anything else — they name the affected member directly.
2. Break the error rate down by the alert's grouping dimensions to find whether the errors are concentrated or spread. Concentrated is a code or endpoint problem; spread is usually a dependency or platform problem.
3. Read the actual error responses, not just the count — the status code and message usually name the cause.
4. Follow a failing trace end to end and find the first span that errors. That span, not the one that alerted, is where the fault is.
5. Compare against the deploy timeline for this service and its dependencies.
6. Read what `diag-api-health` already attached to the alert (top failing routes, upstream health, current rate-limit headroom) before re-running those checks by hand.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `trace.http.request.hits`.

**Logs.** `service:api-services env:prod status:error`

**Traces.** APM > Traces, filtered to `service:api-services env:prod`, sorted by duration or filtered to errored spans.

**Where to look.** APM traces and service pages are the primary surface. Use the service map to see which dependency is contributing the failure, and the endpoint list to find whether it is concentrated on one route.

**Dashboards.** *Domain — API* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `api-gateway`, `api-services`, `partner-apis`. Correlated alerts share the prefix `api-services.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. If a deploy correlates, roll it back before diagnosing further.
2. If one dependency is responsible, enable the circuit breaker or fallback path for that dependency. Blast radius: the feature that depends on it.
3. If a single client or partner is responsible, rate-limit that caller rather than degrading everyone.
4. If credentials expired, rotate and redeploy the secret; do not disable verification.

**Automation.** `diag-api-health` — diagnostic only, read-only, approval: none.
Actions: `fetch_monitor`, `top_failing_endpoints`, `upstream_status`, `rate_limit_state`.
Attached to the alert automatically: top failing routes, upstream health, current rate-limit headroom.

## Rollback procedure

Redeploy the previous release. If a circuit breaker or fallback was enabled, disable it only once the dependency has been error-free for a full evaluation window, and re-enable immediately if errors return.

Whatever is changed, record it on the incident. If `diag-api-health` performed an action, review its run history before making further changes. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when the error rate is back to its pre-incident level (not merely under the threshold), no new error signatures remain, and the SLO has stopped burning.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_30m`).
2. `slo-api-availability` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P2. At P2: acknowledge within 10 minutes, escalate at 20.

1. **Owning team**, `application-development` — notified, not paged.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `application-development` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Application Development** (`application-development`) — appdev@acme.example

- Alert channel: `teams-appdev-alerts` · low-noise: `teams-appdev-signals`
- Escalates to: `sre`
- Domains owned: api, integration
- Support model: business hours

## ServiceNow service and CI

- **Configuration item (CI):** `api-services` — the failure domain this monitor is scoped to.
- **Business service:** `api-services`
- **Assignment group:** `APP_DEVELOPMENT`
- **Record type:** Task. This archetype does not raise an incident automatically.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Application Development (`application-development`) — appdev@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/api.yaml` → `api-4xx-anomaly`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->
