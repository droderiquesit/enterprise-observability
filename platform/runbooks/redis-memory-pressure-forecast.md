# Runbook: Redis Memory Pressure Forecast

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `redis-memory-pressure-forecast` |
| Domain | Azure / Cloud Platform |
| Signal | `capacity` · impact class `risk` |
| Detection | `forecast` |
| Monitor type | `query alert` |
| Environments | prod, stage |
| Priority | P3 · does not page |
| SLO | `slo-cloud-platform-availability` — Azure platform reliability (target 99.9%) |
| Automation | `auto-capacity-ticket` (fully automatic) |
| Grouping | `subscription_name`, `name` · collapse: `subscription_name` |
| Failure domain | `caching` |

## Purpose and monitored condition

This monitor watches **capacity** for `azure_redis` resources in the Azure / Cloud Platform domain. It measures remaining headroom against a hard, physical or contractual limit that cannot be exceeded.

It fires when extrapolating the recent trend, the resource is projected to cross its limit inside the forecast horizon. It has NOT crossed it yet, evaluated over `next_12h`.

Condition as deployed:

```
max(next_12h):forecast(avg:azure.cache_redis.usedmemorypercentage{__SCOPE__} by {subscription_name,name}, 'linear', 1, interval='30m', history='1d') > 85
```

Thresholds: `critical` = 85, `warning` = 75.

Scope selector: `resource_type:azure_redis`. The alert is evaluated per `subscription_name`, `name` and notifications collapse on `subscription_name`.

**Reading the trigger.** The lead time is the entire point — resolving this by waiting for the breach wastes it. Equally, a forecast built on a short-lived spike will retract on its own; confirm the trend is sustained before major action.

**Design note.** Crossing the memory limit does not fail the cache — it starts EVICTING, which is worse because it is silent. Forecasting the limit is what makes eviction a planned resize instead of a mystery latency incident.

## Business and technical impact

**Business impact.** Nothing is broken yet. A boundary will be crossed if nobody acts within the lead time this monitor provides — the cost of ignoring it is that it becomes an outage on somebody else's shift.

**Technical impact.** The system is operating inside its limits but the margin is shrinking. Once the boundary is crossed the failure mode is usually abrupt rather than gradual.

**Error budget.** This condition is associated with `slo-cloud-platform-availability` (Azure platform reliability, target 99.9% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `caching` in `prod`, and anything downstream of `azure-landing-zone`, `azure-paas`, `cloud-quota`.

## Likely causes

Most frequent first.

1. **Sustained growth.** Consumption grew predictably and the limit was not raised in time.
2. **Retention or cleanup stopped.** Old data, snapshots, logs or artifacts are no longer being expired.
3. **A large one-off consumer.** A backfill, migration, import or debug-level logging run consumed the headroom.
4. **A quota or limit reduction.** The ceiling moved down rather than usage moving up.

## Investigation steps

1. Read the alert's group tags (`subscription_name`, `name`) to establish scope before opening anything else — they name the affected member directly.
2. Confirm the limit and the current value — the alert is about the ratio, and both sides of it can move.
3. Identify the largest consumers and whether any are unexpected or reclaimable.
4. Check whether cleanup, retention or expiry jobs are still running successfully.
5. Calculate the time remaining at the current rate to decide between now and next business day.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `azure.cache_redis.usedmemorypercentage`.

**Logs.** `service:caching env:prod source:azure`

**Traces.** APM traces where an application sits in front of the resource.

**Where to look.** The cloud provider integration surfaces resource-level metrics and platform health. Check the provider's own status and the resource's activity log for changes.

**Dashboards.** *Domain — Azure / Cloud Platform* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `azure-landing-zone`, `azure-paas`, `cloud-quota`, `cloud-finops`. Correlated alerts share the prefix `caching.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. Reclaim first: expire, archive or delete what is already safe to remove. Blast radius: none if retention policy is followed.
2. Then raise the limit or add capacity, within the approved budget.
3. Stop or throttle the consumer responsible if it is non-critical.

**Automation.** `auto-capacity-ticket` — fully automatic, approval: none.
Actions: `attach_forecast_chart`, `create_servicenow_task`, `assign_to_owner`.
Rate cap: 50 actions/hour.

## Rollback procedure

If a limit was raised, it can stay — but record it. If data was archived to reclaim space, confirm the archive is readable before deleting the source, and restore from archive if anything downstream breaks.

Whatever is changed, record it on the incident. If `auto-capacity-ticket` performed an automatic action, it is reversible by design — check its run history for the compensating action. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when headroom is back above the policy threshold, the reclaim or expansion is permanent rather than manual, and the projection no longer breaches.

1. The monitor returns to OK and stays there for at least one full evaluation window (`next_12h`).
2. `slo-cloud-platform-availability` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P3. At P3: acknowledge within 240 minutes, escalate at 1440.

1. **Owning team**, `cloud-engineering` — notified, not paged.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `cloud-engineering` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Cloud Engineering** (`cloud-engineering`) — cloud@acme.example

- Alert channel: `teams-cloud-alerts` · low-noise: `teams-cloud-signals`
- Escalates to: `sre`
- Domains owned: cloud, kubernetes, messaging
- Support model: business hours

## ServiceNow service and CI

- **Configuration item (CI):** `caching` — the failure domain this monitor is scoped to.
- **Business service:** `azure-landing-zone`
- **Assignment group:** `CLOUD_PLATFORM`
- **Record type:** Task. This archetype does not raise an incident automatically.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Cloud Engineering (`cloud-engineering`) — cloud@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/cloud.yaml` → `redis-memory-pressure-forecast`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->

