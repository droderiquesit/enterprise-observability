# Runbook: Authentication Failure Anomaly

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `auth-failure-anomaly` |
| Domain | Security Operational Signals (owner `security`) |
| Signal | `auth_anomaly` · impact class `degradation` |
| Detection | `seasonal_anomaly` |
| Environments | prod |
| Priority | P2, P3 · does not page |
| SLO | `slo-security-telemetry` — Security telemetry continuity (target 99.5%) |
| Automation | `diag-security-control` (diagnostic only) |
| Grouping | `identity_provider`, `application` · collapse: none |
| Failure domain | `identity-security` (correlation key prefix) |

## Meaning

This monitor fired because the signal deviated from its learned SEASONAL baseline — same hour, same day of week. Normal daily and weekly cycles are already accounted for.

Query:

```
avg(last_30m):anomalies(sum:acme.identity.auth_failures{__SCOPE__} by {identity_provider,application}.as_rate(), 'robust', 3, direction='above', seasonality='weekly') >= 1
```

**Design note.** Weekly seasonality matters here: Monday morning login failures are normal, the same volume at 03:00 Sunday is not.

## Impact

The service still functions but measurably worse; a subset of traffic or users is failing.

Error budget: this condition is associated with `slo-security-telemetry`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `diag-security-control` (diagnostic only) runs automatically on every alert.

Actions: `fetch_monitor`, `control_last_success`, `source_agent_state`, `related_signals`.

Attached to the alert automatically: last successful control run, agent state, related Cloud SIEM signals.

## Escalation

1. Owning team: `security` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `security-leadership`.
3. Priority contract: P2, P3 — ack within 10 minutes, escalate at 20.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `identity-security`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

