# Runbook: API Gateway Backend Pool Unhealthy

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `api-gateway-backend-unhealthy` |
| Domain | API |
| Signal | `availability` · impact class `customer_impact` |
| Detection | `threshold` |
| Monitor type | `query alert` |
| Environments | prod, stage |
| Priority | P1, P2, P3 · **pages the on-call rotation** |
| SLO | `slo-api-availability` — API availability (target 99.95%) |
| Automation | `diag-api-health` (diagnostic only) |
| Grouping | `gateway`, `backend_pool` · collapse: none |
| Failure domain | `api-gateway` |

## Purpose and monitored condition

This monitor watches **availability** for `api_gateway` resources in the API domain. It measures whether the resource answers at all — a binary up/down or a success-ratio collapse, not a slow-down.

It fires when a fixed threshold was crossed, evaluated over `last_5m`.

Condition as deployed:

```
min(last_5m):avg:api.gateway.healthy_backend_ratio{__SCOPE__} by {gateway,backend_pool} < 0.5
```

Thresholds: `critical` = 0.5, `warning` = 0.75.

Scope selector: `resource_type:api_gateway`. The alert is evaluated per `gateway`, `backend_pool`.

**Reading the trigger.** A fixed threshold is a judgement about what matters, not a law of nature. If the value is barely over and the trend is flat, the threshold may be the problem — raise that through review rather than muting the alert.

**Why this threshold is a fixed number.** Losing half of a backend pool is a redundancy boundary defined by the load-balancer contract, not a statistical property of traffic.

## Business and technical impact

**Business impact.** Customers are affected now, or a mission-critical capability is unavailable. Treat as an outage until proven otherwise.

**Technical impact.** Requests are failing or the capability is unavailable. Dependent services will begin timing out, retrying and shedding load, so the blast radius grows the longer it runs.

**Error budget.** This condition is associated with `slo-api-availability` (API availability, target 99.95% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `api-gateway` in `prod`, and anything downstream of `api-gateway`, `api-services`, `partner-apis`.

## Likely causes

Most frequent first.

1. **Process or instance died.** The workload crashed, was OOM-killed, or the host/VM was terminated. Check restart counts and exit codes before anything else.
2. **A dependency it cannot start without.** Database, secret store, config service or identity provider is unreachable, so the resource fails its own readiness check.
3. **A deployment or configuration change.** A rollout replaced a working version with one that cannot bind, cannot authenticate, or fails health checks.
4. **Network or DNS path loss.** The resource is alive but unreachable — security group, route, firewall rule, certificate or DNS record changed.
5. **Capacity exhaustion.** Connection pool, file handles, disk or memory hit a hard limit and the resource stopped accepting work.

## Investigation steps

1. Read the alert's group tags (`gateway`, `backend_pool`) to establish scope before opening anything else — they name the affected member directly.
2. Confirm the scope from the alert's group tags: one member down is a member problem, all members down is a dependency or platform problem.
3. Check whether the resource restarted — a restart loop and a clean outage need different responses.
4. Check the correlated change events attached to this alert. Most availability loss is change-induced and the fastest fix is to reverse the change.
5. Test the dependency path the resource needs to become ready, in the order it needs them.
6. Read what `diag-api-health` already attached to the alert (top failing routes, upstream health, current rate-limit headroom) before re-running those checks by hand.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `api.gateway.healthy_backend_ratio`.

**Logs.** `service:api-gateway env:prod status:error`

**Traces.** APM > Traces, filtered to `service:api-gateway env:prod`, sorted by duration or filtered to errored spans.

**Where to look.** APM traces and service pages are the primary surface. Use the service map to see which dependency is contributing the failure, and the endpoint list to find whether it is concentrated on one route.

**Dashboards.** *Domain — API* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `api-gateway`, `api-services`, `partner-apis`. Correlated alerts share the prefix `api-gateway.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. If a change correlates, roll it back. This is the highest-yield action and needs no further diagnosis.
2. If a single member is down and the workload is replicated, remove it from the pool and let the platform replace it. Blast radius: one member.
3. If a dependency is down, fail over to the standby or enable the degraded path. Record the decision on the incident.
4. Restart the resource only after capturing diagnostics — a restart destroys the evidence that explains the outage.

**Automation.** `diag-api-health` — diagnostic only, read-only, approval: none.
Actions: `fetch_monitor`, `top_failing_endpoints`, `upstream_status`, `rate_limit_state`.
Attached to the alert automatically: top failing routes, upstream health, current rate-limit headroom.

## Rollback procedure

Re-deploy the previously known-good version, or restore the member to the pool. If a failover was performed, fail back only after the primary has been healthy for a full evaluation window — flapping between primary and standby is worse than staying on the standby.

Whatever is changed, record it on the incident. If `diag-api-health` performed an action, review its run history before making further changes. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when the resource reports healthy for a full evaluation window, its dependents have stopped erroring, and the replacement or restored member is serving traffic.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_5m`).
2. `slo-api-availability` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P1, P2, P3. At P1: acknowledge within 5 minutes, escalate at 10.

1. **Primary on-call**, `application-development` rotation — paged immediately.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `application-development` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Application Development** (`application-development`) — appdev@acme.example

- Alert channel: `teams-appdev-alerts` · low-noise: `teams-appdev-signals`
- Escalates to: `sre`
- Domains owned: api, integration
- Support model: 24x7

## ServiceNow service and CI

- **Configuration item (CI):** `api-gateway` — the failure domain this monitor is scoped to.
- **Business service:** `api-services`
- **Assignment group:** `APP_DEVELOPMENT`
- **Record type:** Incident (P1 → SEV-1, P2 → SEV-2) in production; Task in qa/stage.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Application Development (`application-development`) — appdev@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/api.yaml` → `api-gateway-backend-unhealthy`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->
