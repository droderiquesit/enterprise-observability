# Runbook: DNS Resolution Failing

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `dns-resolution-failure` |
| Domain | Networking (owner `infrastructure-engineering`) |
| Signal | `availability` · impact class `customer_impact` |
| Detection | `threshold` |
| Environments | prod |
| Priority | P1, P2 · **pages the on-call rotation** |
| SLO | `slo-network-availability` — Network availability (target 99.95%) |
| Automation | `diag-network` (diagnostic only) |
| Grouping | `zone`, `resolver` · collapse: `zone` |
| Failure domain | `dns` (correlation key prefix) |

## Meaning

This monitor fired because a fixed threshold was crossed.

Query:

```
sum(last_10m):sum:synthetics.dns.failures{__SCOPE__} by {zone,resolver}.as_count() >= 2
```

**Why this threshold is a fixed number.** DNS failure is binary and upstream of everything — this archetype is a designated correlation PARENT for the whole estate.

## Impact

Customers are affected now, or a mission-critical capability is unavailable. Treat as an outage until proven otherwise.

Error budget: this condition is associated with `slo-network-availability`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `diag-network` (diagnostic only) runs automatically on every alert.

Actions: `fetch_monitor`, `path_traceroute`, `device_interface_state`, `recent_config_changes`.

Attached to the alert automatically: traceroute, interface counters, config diffs in the last 24h.

## Escalation

1. Owning team: `infrastructure-engineering` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `sre`.
3. Priority contract: P1, P2 — ack within 5 minutes, escalate at 10.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `dns`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

