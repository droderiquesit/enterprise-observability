# Runbook: Data Volume Anomaly

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `pipeline-volume-anomaly` |
| Domain | Data Platform |
| Signal | `volume` · impact class `customer_impact` |
| Detection | `anomaly` |
| Monitor type | `query alert` |
| Environments | prod |
| Priority | P1 · **pages the on-call rotation** |
| SLO | `slo-data-freshness` — Data product freshness (target 99.0%) |
| Automation | `diag-data-pipeline` (diagnostic only) |
| Grouping | `data_product` · collapse: none |
| Failure domain | `data-quality` |

## Purpose and monitored condition

This monitor watches **volume** for `data_product` resources in the Data Platform domain. It measures whether the amount of data arriving matches what the pipeline normally receives.

It fires when the signal deviated from its own learned baseline by more than the configured number of deviations. It did NOT cross a fixed number, evaluated over `last_2h`.

Condition as deployed:

```
avg(last_2h):anomalies(sum:acme.data.rows_written{__SCOPE__} by {data_product}.as_count(), 'robust', 3, seasonality='weekly') >= 1
```

Thresholds: `critical` = 1.0.

Scope selector: ``. The alert is evaluated per `data_product`.

**Reading the trigger.** 'The value looks normal to me' is not a reason to close this — the baseline is what moved. Conversely, a recent step change in traffic can make a healthy service look anomalous for a day while the baseline relearns.

**Design note.** Catches the silent failure mode that job-status monitoring cannot see: the job succeeds and writes 4 rows instead of 4 million.

## Business and technical impact

**Business impact.** Customers are affected now, or a mission-critical capability is unavailable. Treat as an outage until proven otherwise.

**Technical impact.** Requests are failing or the capability is unavailable. Dependent services will begin timing out, retrying and shedding load, so the blast radius grows the longer it runs.

**Error budget.** This condition is associated with `slo-data-freshness` (Data product freshness, target 99.0% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `data-quality` in `prod`, and anything downstream of `data-pipelines`, `data-warehouse`, `data-quality`.

## Likely causes

Most frequent first.

1. **An upstream producer stopped or reduced output.** A partial feed is the usual cause of a volume drop.
2. **A filter or routing change.** Records are being dropped or routed elsewhere before they arrive.
3. **A genuine business change.** A real drop in activity — seasonality, a closed region, a retired client.
4. **Duplicate delivery.** For a volume spike, the same batch delivered twice.

## Investigation steps

1. Read the alert's group tags (`data_product`) to establish scope before opening anything else — they name the affected member directly.
2. Compare against the same period last week before treating this as a defect — volume is the signal most often explained by seasonality.
3. Break the volume down by source to find whether one producer or all of them changed.
4. Check for recent filter, routing or sampling changes in the ingestion path.
5. For a spike, check for duplicate batch identifiers before reprocessing anything.
6. Read what `diag-data-pipeline` already attached to the alert (last run log tail, upstream freshness, row-count delta vs baseline) before re-running those checks by hand.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `acme.data.rows_written`.

**Logs.** `service:data-quality env:prod source:pipeline`

**Traces.** Not usually applicable; the run history and job output are the equivalent trail.

**Where to look.** Pipeline run history, per-run duration and record counts. The run's own logs name the failure more precisely than any metric.

**Dashboards.** *Domain — Data Platform* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `data-pipelines`, `data-warehouse`, `data-quality`. Correlated alerts share the prefix `data-quality.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. If a producer stopped, escalate to its owner — the fix is upstream.
2. If a filter change is responsible, reverse it.
3. If duplicates arrived, de-duplicate before the data reaches consumers; do not let downstream aggregate twice.

**Automation.** `diag-data-pipeline` — diagnostic only, read-only, approval: none.
Actions: `fetch_monitor`, `last_run_logs`, `upstream_dataset_freshness`, `row_count_delta`.
Attached to the alert automatically: last run log tail, upstream freshness, row-count delta vs baseline.

## Rollback procedure

Reversing a filter restores volume immediately. If de-duplication removed records, verify the retained set is complete against the source manifest before closing.

Whatever is changed, record it on the incident. If `diag-data-pipeline` performed an action, review its run history before making further changes. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when volume is back inside its expected band for the period, per-source counts reconcile against the producer's manifest, and no consumer double-counted.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_2h`).
2. `slo-data-freshness` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P1. At P1: acknowledge within 5 minutes, escalate at 10.

1. **Primary on-call**, `data-engineering` rotation — paged immediately.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `data-engineering` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Data Engineering** (`data-engineering`) — data@acme.example

- Alert channel: `teams-data-alerts` · low-noise: `teams-data-signals`
- Escalates to: `sre`
- Domains owned: database, data
- Support model: 24x7

## ServiceNow service and CI

- **Configuration item (CI):** `data-quality` — the failure domain this monitor is scoped to.
- **Business service:** `data-platform`
- **Assignment group:** `DATA_PLATFORM`
- **Record type:** Incident (P1 → SEV-1, P2 → SEV-2) in production; Task in qa/stage.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Data Engineering (`data-engineering`) — data@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/data.yaml` → `pipeline-volume-anomaly`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->
