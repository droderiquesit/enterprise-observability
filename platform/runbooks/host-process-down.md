# Runbook: Required Process Not Running

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `host-process-down` |
| Domain | Infrastructure (host / VM / storage) (owner `infrastructure-engineering`) |
| Signal | `availability` · impact class `degradation` |
| Detection | `service_check` |
| Environments | prod, stage |
| Priority | P2, P3 · does not page |
| SLO | `slo-infra-compute-availability` — Compute platform availability (target 99.9%) |
| Automation | `diag-host` (diagnostic only) |
| Grouping | `cluster`, `host`, `process_name` · collapse: `cluster` |
| Failure domain | `compute` (correlation key prefix) |

## Meaning

This monitor fired because a check reported a non-OK status for consecutive evaluations.

Query:

```
"process.up".over("__SCOPE__").by("cluster","host","process_name").last(3).count_by_status()
```

**Why this threshold is a fixed number.** Process presence is binary.

## Impact

The service still functions but measurably worse; a subset of traffic or users is failing.

Error budget: this condition is associated with `slo-infra-compute-availability`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `diag-host` (diagnostic only) runs automatically on every alert.

Actions: `fetch_monitor`, `host_process_list`, `disk_usage_breakdown`, `recent_host_events`.

Attached to the alert automatically: top processes, per-mount usage, host event timeline.

## Escalation

1. Owning team: `infrastructure-engineering` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `sre`.
3. Priority contract: P2, P3 — ack within 10 minutes, escalate at 20.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `compute`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

