service:
  name: nightly-settlement
  team: payments
  tier: tier1
  service_archetype: batch_job
  description: Nightly settlement and reconciliation run; feeds finance.
  envs: [qa, stage, prod]
  dependencies: [orders-sql, partner-sftp]
  idempotent: true
  links:
    - { name: Repository, type: repo, url: https://github.com/acme/nightly-settlement }
