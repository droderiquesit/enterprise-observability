# Runbook: Batch Output Not Produced In Window

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `batch-output-freshness` |
| Domain | Integration Platforms & Batch (owner `application-development`) |
| Signal | `freshness` · impact class `customer_impact` |
| Detection | `threshold` |
| Environments | prod |
| Priority | P1, P2 · **pages the on-call rotation** |
| SLO | `slo-batch-completion` — Batch completion reliability (target 99.0%) |
| Automation | `remediate-rerun-job` (approval required) |
| Grouping | `service`, `job_name` · collapse: none |
| Failure domain | `batch-platform` (correlation key prefix) |

## Meaning

This monitor fired because a fixed threshold was crossed.

Query:

```
max(last_1h):max:acme.batch.hours_since_success{__SCOPE__} by {service,job_name} > 26
```

**Why this threshold is a fixed number.** Daily batches have a contractual delivery time; 26 hours means yesterday's output never arrived. The number is the SLA.

## Impact

Customers are affected now, or a mission-critical capability is unavailable. Treat as an outage until proven otherwise.

Error budget: this condition is associated with `slo-batch-completion`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `remediate-rerun-job` (approval required) runs when triggered.

Actions: `check_idempotency_flag`, `request_approval`, `trigger_rerun`, `verify_completion`.

Guardrails before anything is changed:

- REFUSES unless the job declares idempotent: true in platform/services/<svc>.yaml
- refuses if a downstream consumer is mid-read
- single retry only; a second failure escalates to a human

## Escalation

1. Owning team: `application-development` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `sre`.
3. Priority contract: P1, P2 — ack within 5 minutes, escalate at 10.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `batch-platform`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

