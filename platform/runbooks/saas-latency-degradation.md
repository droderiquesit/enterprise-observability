# Runbook: SaaS Dependency Latency Degradation

<!-- AUTOGENERATED:BEGIN -->
*This block is generated from `platform/policy/archetypes/` by
`tools/generate_runbooks.py`. Edit the catalog, not this text. Sections below
the marker are yours.*

| | |
|---|---|
| Archetype | `saas-latency-degradation` |
| Domain | SaaS Dependencies & External Endpoints (owner `sre`) |
| Signal | `latency` · impact class `degradation` |
| Detection | `anomaly` |
| Environments | prod |
| Priority | P2 · does not page |
| SLO | `slo-external-dependency` — External dependency availability (target 99.5%) |
| Automation | `diag-dependency` (diagnostic only) |
| Grouping | `service`, `peer.service` · collapse: none |
| Failure domain | `saas-dependency` (correlation key prefix) |

## Meaning

This monitor fired because the signal deviated from its own learned baseline by more than the configured number of deviations. It did NOT cross a fixed number, so 'the value looks normal to me' is not a reason to close this.

Query:

```
avg(last_30m):anomalies(avg:trace.http.client.request.duration{__SCOPE__,peer.dependency_kind:external} by {service,peer.service}, 'agile', 3, direction='above') >= 1
```

## Impact

The service still functions but measurably worse; a subset of traffic or users is failing.

Error budget: this condition is associated with `slo-external-dependency`. If the alert is sustained, expect burn-rate alerts on that objective to follow.

## Automation

Workflow `diag-dependency` (diagnostic only) runs automatically on every alert.

Actions: `fetch_monitor`, `peer_service_health`, `circuit_breaker_state`, `vendor_status_page`.

Attached to the alert automatically: peer service SLO state, breaker state, vendor status page result.

## Escalation

1. Owning team: `sre` (see `platform/policy/teams.yaml` for the channel and ServiceNow assignment group).
2. Escalates to: `platform-leadership`.
3. Priority contract: P2 — ack within 10 minutes, escalate at 20.

<!-- AUTOGENERATED:END -->

## Validation

TODO(owner): how does a responder confirm this is real in under two minutes?
Name the dashboard, the query, or the check to run first.

## Likely causes

TODO(owner): the three causes that actually account for most occurrences,
most likely first.

## Diagnostic queries

TODO(owner): copy-pasteable queries. The automation above already attaches the
standard set; list what it cannot.

## Dependency checks

TODO(owner): which upstreams to check for `saas-dependency`, and how to tell
quickly whether the cause is here or upstream.

## Remediation

TODO(owner): the safe fix, with the blast radius stated. If a step is dangerous,
say so and require a second pair of eyes.

## Recovery verification

TODO(owner): what must be true before this is considered resolved, and how to
confirm the error budget stopped burning.

