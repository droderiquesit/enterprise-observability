# Runbook: Cosmos DB Request Unit Throttling

<!-- AUTOGENERATED:BEGIN -->
*Generated from `platform/policy/archetypes/` by `tools/generate_runbooks.py`.*
*Edit the catalog or `tools/runbook_content.py`, not this text. Notes added*
*below the end marker are preserved.*

| | |
|---|---|
| Archetype | `cosmos-ru-throttling` |
| Domain | Databases |
| Signal | `saturation` · impact class `degradation` |
| Detection | `threshold` |
| Monitor type | `query alert` |
| Environments | prod, stage |
| Priority | P2, P3 · does not page |
| SLO | `slo-database-availability` — Database platform availability (target 99.95%) |
| Automation | `remediate-scale-workload` (approval required) |
| Grouping | `subscription_name`, `name` · collapse: `subscription_name` |
| Failure domain | `database-capacity` |

## Purpose and monitored condition

This monitor watches **saturation** for `azure_cosmos_db` resources in the Databases domain. It measures how close a bounded resource is to its limit — the point past which work queues rather than completes.

It fires when a fixed threshold was crossed, evaluated over `last_15m`.

Condition as deployed:

```
sum(last_15m):sum:azure.documentdb_databaseaccounts.total_requests{__SCOPE__,status_code:429} by {subscription_name,name}.as_count() > 0
```

Thresholds: `critical` = 0.

Scope selector: `resource_type:azure_cosmos_db`. The alert is evaluated per `subscription_name`, `name` and notifications collapse on `subscription_name`.

**Reading the trigger.** A fixed threshold is a judgement about what matters, not a law of nature. If the value is barely over and the trend is flat, the threshold may be the problem — raise that through review rather than muting the alert.

**Why this threshold is a fixed number.** HTTP 429 from Cosmos is a hard provisioned-throughput boundary. Any throttling is real customer latency.

## Business and technical impact

**Business impact.** The service still functions but measurably worse; a subset of traffic, users or records is failing or delayed.

**Technical impact.** Error budget is being consumed and latency or failure is propagating to callers. Retries amplify the load, so a degradation can become an outage without any new fault.

**Error budget.** This condition is associated with `slo-database-availability` (Database platform availability, target 99.95% over 30d). Sustained firing consumes that budget and will be followed by burn-rate alerts on the same objective.

**Who feels it.** Consumers of `database-capacity` in `prod`, and anything downstream of `databases`, `database-replication`, `database-capacity`.

## Likely causes

Most frequent first.

1. **Organic growth.** Demand grew into the headroom and the limit was never revisited.
2. **A leak.** Memory, connections, file handles or threads are acquired and never released; the shape is a steady climb that survives traffic troughs.
3. **A change in workload.** A new feature, a batch job, or a retry storm is consuming far more of the resource per unit of work.
4. **Under-provisioning after a scaling change.** An autoscaler bound, node pool size, or instance class was lowered.
5. **A stuck consumer or backlog.** Work is arriving faster than it drains, so the buffer fills.

## Investigation steps

1. Read the alert's group tags (`subscription_name`, `name`) to establish scope before opening anything else — they name the affected member directly.
2. Establish the shape: a step change points at a deployment or config change; a steady climb that ignores traffic troughs points at a leak.
3. Identify the largest consumer using the alert's grouping dimensions before adding capacity — adding capacity to a leak only buys time.
4. Check whether the limit itself changed recently (autoscaler bounds, quotas, instance class, pool size).
5. Project the time to exhaustion so the response is matched to the lead time available.

## Metrics, logs, traces, dashboards and dependencies

**Metrics.** `azure.documentdb_databaseaccounts.total_requests`.

**Logs.** `service:database-capacity env:prod source:database`

**Traces.** APM traces from calling services show the client-side view of the same statements — useful to confirm whether the client or the engine is the bottleneck.

**Where to look.** Database Monitoring shows query-level activity, waits and plan changes. Start from the top queries by total time in the alert window.

**Dashboards.** *Domain — Databases* for this domain's health; *Enterprise Overview* for SLO and platform risk; *On-Call* for what else is paging right now; *Alert Quality Scorecard* if the alert itself looks wrong.

**Dependencies.** Failure domains in scope: `databases`, `database-replication`, `database-capacity`. Correlated alerts share the prefix `database-capacity.<env>.<service>`, so a platform cause will already have adopted the application symptoms as children.

## Safe remediation and automation

Least destructive first. Stop when the signal recovers — every further step widens the blast radius.

1. If there is real headroom to buy, scale out or raise the bound — this is the safe, reversible action and it buys diagnosis time.
2. If a leak is confirmed, recycle the affected members on a rolling basis to reclaim the resource. Blast radius: brief per-member capacity loss.
3. Shed or defer the largest non-critical consumer (batch, backfill, report) to protect interactive traffic.
4. Fix the leak or the workload change; scaling is mitigation, not resolution.

**Automation.** `remediate-scale-workload` — approval required, approval: owner.
Actions: `compute_target_size`, `estimate_cost`, `request_approval`, `apply_scale`, `verify`.
Rate cap: 6 actions/hour.

Guardrails enforced before anything changes:

- hard ceiling from the service's declared max capacity
- refuses if the increase would exceed the monthly cost guardrail
- always scales UP only; scale-down is manual

## Rollback procedure

Return the bound, replica count or quota to its previous documented value once consumption is back in band. Record the new baseline if the higher value is being kept, so the next reviewer does not treat it as drift.

Whatever is changed, record it on the incident. If `remediate-scale-workload` performed an automatic action, it is reversible by design — check its run history for the compensating action. If the remediation makes the signal worse, revert it before trying the next step: two simultaneous changes make the cause unknowable.

## Recovery validation

Resolved when utilisation is back inside its normal band with the original limits restored, the projection no longer crosses the limit inside the forecast horizon, and any deferred work has been drained.

1. The monitor returns to OK and stays there for at least one full evaluation window (`last_15m`).
2. `slo-database-availability` has stopped burning; record the budget consumed against the incident.
3. Any downtime, silence or circuit breaker applied during the incident is removed.
4. The correlation group closes — do not close it while a child alert is still firing.

## Escalation path

This archetype runs at P2, P3. At P2: acknowledge within 10 minutes, escalate at 20.

1. **Owning team**, `data-engineering` — notified, not paged.
2. **Secondary on-call** — after 10 minutes without acknowledgement.
3. **Team lead**, `data-engineering` — after 20 minutes.
4. **Incident commander / platform leadership** (`sre`) — after 30 minutes, or immediately if the impact is confirmed and widening.

Escalate early rather than late if the blast radius is growing, if two or more domains are alerting at once, or if the remediation requires a change nobody present is authorised to approve.

## Owning team

**Data Engineering** (`data-engineering`) — data@acme.example

- Alert channel: `teams-data-alerts` · low-noise: `teams-data-signals`
- Escalates to: `sre`
- Domains owned: database, data
- Support model: business hours

## ServiceNow service and CI

- **Configuration item (CI):** `database-capacity` — the failure domain this monitor is scoped to.
- **Business service:** `database-platform`
- **Assignment group:** `DATA_PLATFORM`
- **Record type:** Task. This archetype does not raise an incident automatically.

ServiceNow records are raised by the routing rules from the monitor's tags — do not open one by hand for the same alert, or the incident will be duplicated.

## Runbook owner and review date

- **Runbook owner:** Data Engineering (`data-engineering`) — data@acme.example
- **Reviewed by:** observability-platform (content standard and attachment)
- **Review cadence:** monthly
- **Next review due:** 2026-11-16
- **Source of truth:** `platform/policy/archetypes/database.yaml` → `cosmos-ru-throttling`

This runbook is published to Datadog as a notebook and attached to every monitor generated from this archetype through the monitor's native `assets` field. It is regenerated on every catalog change, so corrections belong in the catalog or the content library, never in the published notebook.

<!-- AUTOGENERATED:END -->
